# Equinozio App — Tema 1 (Completare l'abbozzato) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Completare le funzioni già abbozzate dell'app iOS Equinozio: campo `pensiero` nella Riflessione, generazione e visualizzazione di Insight nella Mappa, suggerimento tag anche in modifica del Diario, modifica delle riflessioni passate. (Il "radar live" della Decisione è già funzionante — nessun intervento.)

**Architecture:** La logica pura (formula equilibrio, generazione insight) vive in tipi testabili nel layer Domain e si verifica con unit test Swift Testing. Il wiring SwiftUI si verifica via build. Per permettere ai test app-hosted di girare sul simulatore senza iCloud (il container CloudKit altrimenti fa crashare l'host), l'app usa uno store in-memory quando rileva il test runner.

**Tech Stack:** SwiftUI, SwiftData (+CloudKit), Swift Testing (`import Testing`), Xcode 26 / iOS 18 deployment. Lavorare su `main`.

**Spec:** `/Users/giuseppe/XCode/Equinozio/docs/superpowers/specs/2026-06-03-equinozio-landing-e-app-design.md` (sezione 4, Tema 1).

---

## Convenzioni di verifica

- **Build:** `xcodebuild -scheme Equinozio -destination 'generic/platform=iOS Simulator' -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -4` → atteso `** BUILD SUCCEEDED **`.
- **Test:** `xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -30` → atteso esito con i test verdi. (`-only-testing:EquinozioTests` esclude gli UI test, che lancerebbero l'app reale.)
- Design system: usare i token `S.x*`, `R.r*`, `Color.*`, `Font.equinozio(...)`. Helvetica, pesi light/thin/medium — **mai bold**.

## File Structure

**Creati:**
- `Equinozio/Domain/GeneratoreInsight.swift` — logica pura di generazione insight (`InsightGenerato` + `enum GeneratoreInsight`).
- `Equinozio/Features/Mappa/BloccoInsight.swift` — vista che mostra gli insight nella Mappa.
- `Equinozio/Features/Riflessione/ModificaRiflessioneView.swift` — sheet di modifica di una riflessione passata.
- `EquinozioTests/RiflessioneTests.swift` — formula equilibrio + round-trip SwiftData (pensiero, modifica).
- `EquinozioTests/GeneratoreInsightTests.swift` — test delle quattro regole di insight.

**Modificati:**
- `Equinozio/EquinozioApp.swift` — store in-memory sotto test runner.
- `Equinozio/Domain/Modelli.swift` — `Riflessione.equilibrio(...)` statico (DRY).
- `Equinozio/Features/Riflessione/RiflessioneView.swift` — TextEditor pensiero + load/save; usa la formula statica.
- `Equinozio/Features/Riflessione/StoricoRiflessioniView.swift` — righe toccabili → sheet modifica.
- `Equinozio/Features/Mappa/MappaView.swift` — calcolo + visualizzazione insight.
- `Equinozio/Features/Diario/DiarioView.swift` — bottone "SUGGERISCI" in modifica (`DettaglioPaginaView`).

---

## Task 1: Abilitare i test (store in-memory sotto test runner)

I test sono app-hosted: lanciano l'app, il cui `ModelContainer` CloudKit fa crashare l'host sul simulatore senza iCloud. Soluzione: sotto test usare uno store in-memory.

**Files:**
- Modify: `Equinozio/EquinozioApp.swift:22-42`

- [ ] **Step 1: Sostituire la creazione del `modelContainer`**

In `Equinozio/EquinozioApp.swift`, sostituire l'intera proprietà `var modelContainer: ModelContainer = { ... }()` con:

```swift
    var modelContainer: ModelContainer = {
        let schema = Schema([
            Profilo.self,
            Cerchio.self,
            Elemento.self,
            Pagina.self,
            Riflessione.self,
            Decisione.self,
            Insight.self,
        ])

        // Sotto il test runner usiamo uno store in-memory: evita l'inizializzazione
        // di CloudKit (che, senza account iCloud, fa crashare l'host dei test sul
        // simulatore) e isola ogni esecuzione dei test.
        let inTest = ProcessInfo.processInfo.environment["XCTestConfigurationFilePath"] != nil

        let config: ModelConfiguration = inTest
            ? ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)
            : ModelConfiguration(
                schema: schema,
                isStoredInMemoryOnly: false,
                cloudKitDatabase: .private("iCloud.it.systema360.equinozio")
            )

        do {
            return try ModelContainer(for: schema, configurations: [config])
        } catch {
            fatalError("Impossibile creare il ModelContainer: \(error)")
        }
    }()
```

- [ ] **Step 2: Verificare che la suite esistente giri (non più crash dell'host)**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -30`
Expected: la suite **gira** e il test `example` esistente passa (`Test Suite ... passed` / `TEST SUCCEEDED`). Prima di questa modifica l'host crashava all'avvio (CloudKit).

> Contingenza: se l'host crasha ancora perché sotto Swift Testing `XCTestConfigurationFilePath` non risultasse impostato, aggiungere in alternativa il rilevamento `|| ProcessInfo.processInfo.environment["EQUINOZIO_INMEMORY"] == "1"` e passare quella env var nella Test action dello scheme condiviso (`Equinozio.xcodeproj/xcshareddata/xcschemes/Equinozio.xcscheme`). Non modificarlo se lo Step 2 è già verde.

- [ ] **Step 3: Commit**

```bash
cd /Users/giuseppe/XCode/Equinozio
git add Equinozio/EquinozioApp.swift
git commit -m "test: store in-memory sotto test runner per abilitare i test app-hosted"
```

---

## Task 2: Formula equilibrio statica e testabile (DRY)

La formula dell'equilibrio è duplicata (modello `Riflessione.equilibrio` e `RiflessioneView.equilibrioCorrente`). La centralizziamo in un metodo statico testabile.

**Files:**
- Test: `EquinozioTests/RiflessioneTests.swift`
- Modify: `Equinozio/Domain/Modelli.swift:116-122`
- Modify: `Equinozio/Features/Riflessione/RiflessioneView.swift:255-260`

- [ ] **Step 1: Scrivere il test che fallisce**

Create `EquinozioTests/RiflessioneTests.swift`:

```swift
import Testing
import SwiftData
@testable import Equinozio

struct RiflessioneTests {

    @Test func equilibrioPerfetto() {
        #expect(Riflessione.equilibrio(passione: 25, talento: 25, missione: 25, professione: 25) == 100)
    }

    @Test func equilibrioSbilanciato() {
        // scarti [15,5,5,5] = 30, /4 = 7, 100 - 14 = 86
        #expect(Riflessione.equilibrio(passione: 40, talento: 20, missione: 20, professione: 20) == 86)
    }

    @Test func equilibrioEstremo() {
        // scarti [75,25,25,25] = 150, /4 = 37, 100 - 74 = 26
        #expect(Riflessione.equilibrio(passione: 100, talento: 0, missione: 0, professione: 0) == 26)
    }

    @Test func equilibrioNonNegativo() {
        #expect(Riflessione.equilibrio(passione: 100, talento: 0, missione: 0, professione: 0) >= 0)
    }
}
```

- [ ] **Step 2: Eseguire il test e verificare che fallisca (compilazione)**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: FALLISCE in compilazione — `Riflessione.equilibrio(passione:...)` non esiste ancora.

- [ ] **Step 3: Aggiungere il metodo statico e farne uso nel modello**

In `Equinozio/Domain/Modelli.swift`, sostituire la computed property esistente:

```swift
    public var equilibrio: Int {
        let scarti = [quotaPassione, quotaTalento, quotaMissione, quotaProfessione]
            .map { abs($0 - 25) }
        let scartoMedio = scarti.reduce(0, +) / 4
        return max(0, 100 - scartoMedio * 2)
    }
```

con:

```swift
    public var equilibrio: Int {
        Riflessione.equilibrio(
            passione: quotaPassione,
            talento: quotaTalento,
            missione: quotaMissione,
            professione: quotaProfessione
        )
    }

    /// Formula dell'equilibrio: 100 meno il doppio dello scarto medio da 25/25/25/25.
    public static func equilibrio(passione: Int, talento: Int, missione: Int, professione: Int) -> Int {
        let scarti = [passione, talento, missione, professione].map { abs($0 - 25) }
        let scartoMedio = scarti.reduce(0, +) / 4
        return max(0, 100 - scartoMedio * 2)
    }
```

- [ ] **Step 4: Usare la formula statica nella view**

In `Equinozio/Features/Riflessione/RiflessioneView.swift`, sostituire:

```swift
    private var equilibrioCorrente: Int {
        let scarti = [quotePassione, quoteTalento, quoteMissione, quoteProfessione]
            .map { abs($0 - 25) }
        let scartoMedio = scarti.reduce(0, +) / 4
        return max(0, 100 - scartoMedio * 2)
    }
```

con:

```swift
    private var equilibrioCorrente: Int {
        Riflessione.equilibrio(
            passione: quotePassione,
            talento: quoteTalento,
            missione: quoteMissione,
            professione: quoteProfessione
        )
    }
```

- [ ] **Step 5: Eseguire i test e verificare che passino**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: i 4 test di equilibrio passano.

- [ ] **Step 6: Commit**

```bash
git add Equinozio/Domain/Modelli.swift Equinozio/Features/Riflessione/RiflessioneView.swift EquinozioTests/RiflessioneTests.swift
git commit -m "refactor: formula equilibrio statica e testabile (DRY) + test"
```

---

## Task 3: Campo `pensiero` nella Riflessione

Il modello `Riflessione.pensiero` esiste e lo Storico lo mostra già (riga 248), ma manca la UI di input.

**Files:**
- Test: `EquinozioTests/RiflessioneTests.swift` (estende)
- Modify: `Equinozio/Features/Riflessione/RiflessioneView.swift`

- [ ] **Step 1: Scrivere il test di round-trip che fallisce**

Aggiungere a `EquinozioTests/RiflessioneTests.swift`, dentro lo `struct RiflessioneTests`:

```swift
    @MainActor
    @Test func pensieroPersisteSuSwiftData() throws {
        let schema = Schema([Profilo.self, Cerchio.self, Elemento.self, Pagina.self, Riflessione.self, Decisione.self, Insight.self])
        let container = try ModelContainer(
            for: schema,
            configurations: [ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)]
        )
        let ctx = container.mainContext

        let r = Riflessione(data: .now, quotaPassione: 25, quotaTalento: 25, quotaMissione: 25, quotaProfessione: 25, pensiero: "Settimana intensa")
        ctx.insert(r)
        try ctx.save()

        let lette = try ctx.fetch(FetchDescriptor<Riflessione>())
        #expect(lette.count == 1)
        #expect(lette.first?.pensiero == "Settimana intensa")
    }
```

- [ ] **Step 2: Eseguire e verificare il fallimento atteso**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests/RiflessioneTests/pensieroPersisteSuSwiftData CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: il test passa SOLO se il modello supporta `pensiero` (già presente). Se passa subito, è il guard di regressione dello schema voluto; procedere comunque con la UI sotto. Se fallisce, leggere l'errore e correggere prima di proseguire.

> Nota: questo test verifica il modello (già pronto). Il valore reale del task è il wiring UI degli step seguenti, verificato via build.

- [ ] **Step 3: Aggiungere lo stato e l'input pensiero nella view**

In `Equinozio/Features/Riflessione/RiflessioneView.swift`:

(a) aggiungere lo stato accanto agli altri `@State` (dopo `@State private var caricato: Bool = false`):

```swift
    @State private var pensiero: String = ""
```

(b) sostituire la computed `pensieroGuidato` (righe 234-247) con:

```swift
    private var pensieroGuidato: some View {
        VStack(alignment: .leading, spacing: S.x2) {
            Text("PENSIERO GUIDATO")
                .font(.equinozio(.etichetta))
                .tracking(1.8)
                .foregroundStyle(Color.salvia)

            Text("Mentre rifletti su questa settimana, dove hai sentito più presenza?")
                .font(.equinozio(.corpo))
                .foregroundStyle(Color.inchiostroTenue)

            TextEditor(text: $pensiero)
                .font(.equinozio(.corpo))
                .frame(minHeight: 80)
                .scrollContentBackground(.hidden)
                .padding(S.x2)
                .background(Color.superficie)
                .clipShape(RoundedRectangle(cornerRadius: R.r2))
                .overlay(
                    RoundedRectangle(cornerRadius: R.r2)
                        .stroke(Color.lineaSottile, lineWidth: 1)
                )
                .overlay(alignment: .topLeading) {
                    if pensiero.isEmpty {
                        Text("Una riga, se ti va.")
                            .font(.equinozio(.corpo))
                            .foregroundStyle(Color.attenuato)
                            .padding(.top, S.x2 + 8)
                            .padding(.leading, S.x2 + 5)
                            .allowsHitTesting(false)
                    }
                }
                .accessibilityLabel("Pensiero della settimana")
        }
        .padding(.leading, S.x4)
        .overlay(Rectangle().frame(width: 2).foregroundStyle(Color.salvia), alignment: .leading)
    }
```

(c) in `caricaSettimanaCorrente()` (righe 277-286), dentro l'`if let esistente`, aggiungere dopo l'assegnazione di `quoteProfessione`:

```swift
            pensiero = esistente.pensiero
```

(d) in `salva()` (righe 288-324), impostare il pensiero su entrambi i rami. Nel ramo `if let esistente` aggiungere dopo `esistente.data = inizio`:

```swift
            esistente.pensiero = pensiero.trimmingCharacters(in: .whitespacesAndNewlines)
```

e nel ramo `else` sostituire la creazione di `Riflessione` con (aggiunge il parametro `pensiero`):

```swift
            let nuova = Riflessione(
                data: inizio,
                quotaPassione: quotePassione,
                quotaTalento: quoteTalento,
                quotaMissione: quoteMissione,
                quotaProfessione: quoteProfessione,
                pensiero: pensiero.trimmingCharacters(in: .whitespacesAndNewlines)
            )
            contesto.insert(nuova)
```

- [ ] **Step 4: Build**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild -scheme Equinozio -destination 'generic/platform=iOS Simulator' -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -4`
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 5: Test (round-trip verde)**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: tutti i test verdi.

- [ ] **Step 6: Commit**

```bash
git add Equinozio/Features/Riflessione/RiflessioneView.swift EquinozioTests/RiflessioneTests.swift
git commit -m "feat: cattura del pensiero settimanale nella Riflessione"
```

---

## Task 4: GeneratoreInsight — regole bilanciamentoBasso e dominanzaCerchio (TDD)

Logica pura che trasforma i dati dell'utente in insight. Input: array di model (costruibili nei test senza container).

**Files:**
- Create: `Equinozio/Domain/GeneratoreInsight.swift`
- Test: `EquinozioTests/GeneratoreInsightTests.swift`

- [ ] **Step 1: Scrivere i test che falliscono**

Create `EquinozioTests/GeneratoreInsightTests.swift`:

```swift
import Testing
import Foundation
@testable import Equinozio

struct GeneratoreInsightTests {

    private func rifl(_ p: Int, _ t: Int, _ m: Int, _ s: Int, _ data: Date = .now) -> Riflessione {
        Riflessione(data: data, quotaPassione: p, quotaTalento: t, quotaMissione: m, quotaProfessione: s)
    }

    @Test func bilanciamentoBassoQuandoEquilibrioSottoSoglia() {
        // 70/10/10/10 → equilibrio basso
        let insight = GeneratoreInsight.genera(riflessioni: [rifl(70, 10, 10, 10)], decisioni: [], adesso: .now)
        #expect(insight.contains { $0.tipo == .bilanciamentoBasso })
    }

    @Test func nessunBilanciamentoBassoQuandoEquilibrato() {
        let insight = GeneratoreInsight.genera(riflessioni: [rifl(25, 25, 25, 25)], decisioni: [], adesso: .now)
        #expect(!insight.contains { $0.tipo == .bilanciamentoBasso })
    }

    @Test func dominanzaCerchioQuandoUnaQuotaAlta() {
        let insight = GeneratoreInsight.genera(riflessioni: [rifl(60, 20, 10, 10)], decisioni: [], adesso: .now)
        let dom = insight.first { $0.tipo == .dominanzaCerchio }
        #expect(dom != nil)
        #expect(dom?.testo.contains("Passione") == true)
    }

    @Test func nessunInsightSenzaRiflessioni() {
        let insight = GeneratoreInsight.genera(riflessioni: [], decisioni: [], adesso: .now)
        #expect(insight.isEmpty)
    }
}
```

- [ ] **Step 2: Eseguire e verificare il fallimento (compilazione)**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests/GeneratoreInsightTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: FALLISCE — `GeneratoreInsight` / `InsightGenerato` non esistono.

- [ ] **Step 3: Creare il generatore con le prime due regole**

Create `Equinozio/Domain/GeneratoreInsight.swift`:

```swift
//
//  GeneratoreInsight.swift
//  Equinozio · Domain
//
//  Trasforma i dati dell'utente (riflessioni, decisioni) in insight discreti
//  mostrati nella Mappa. Logica pura e deterministica · testabile in isolamento.
//

import Foundation

public struct InsightGenerato: Identifiable, Equatable {
    public let id = UUID()
    public let tipo: TipoInsight
    public let testo: String

    public init(tipo: TipoInsight, testo: String) {
        self.tipo = tipo
        self.testo = testo
    }

    public static func == (lhs: InsightGenerato, rhs: InsightGenerato) -> Bool {
        lhs.tipo == rhs.tipo && lhs.testo == rhs.testo
    }
}

public enum GeneratoreInsight {

    /// Genera fino a 3 insight, in ordine di priorità.
    /// - riflessioni: ordinate dalla più recente alla più vecchia.
    public static func genera(
        riflessioni: [Riflessione],
        decisioni: [Decisione],
        adesso: Date
    ) -> [InsightGenerato] {
        var risultato: [InsightGenerato] = []

        if let i = bilanciamentoBasso(riflessioni) { risultato.append(i) }
        if let i = dominanzaCerchio(riflessioni) { risultato.append(i) }

        return Array(risultato.prefix(3))
    }

    // MARK: - Regole

    static func bilanciamentoBasso(_ riflessioni: [Riflessione]) -> InsightGenerato? {
        // Soglia 60: la formula equilibrio è "soft" (es. 70/10/10/10 → 56), quindi
        // 60 cattura le settimane realmente sbilanciate senza essere troppo permissiva.
        guard let ultima = riflessioni.first, ultima.equilibrio < 60 else { return nil }
        return InsightGenerato(
            tipo: .bilanciamentoBasso,
            testo: "Questa settimana il tuo equilibrio è \(ultima.equilibrio)%. Prova a riportare un po' di tempo verso i cerchi più trascurati."
        )
    }

    static func dominanzaCerchio(_ riflessioni: [Riflessione]) -> InsightGenerato? {
        guard let ultima = riflessioni.first else { return nil }
        let quote: [(TipoCerchio, Int)] = [
            (.passione, ultima.quotaPassione),
            (.talento, ultima.quotaTalento),
            (.missione, ultima.quotaMissione),
            (.professione, ultima.quotaProfessione),
        ]
        guard let dominante = quote.max(by: { $0.1 < $1.1 }), dominante.1 >= 50 else { return nil }
        return InsightGenerato(
            tipo: .dominanzaCerchio,
            testo: "Stai dedicando molto a \(dominante.0.titolo) (\(dominante.1)%). Va bene, se è una scelta consapevole."
        )
    }
}
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests/GeneratoreInsightTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: i 4 test passano.

- [ ] **Step 5: Commit**

```bash
git add Equinozio/Domain/GeneratoreInsight.swift EquinozioTests/GeneratoreInsightTests.swift
git commit -m "feat: GeneratoreInsight con regole bilanciamentoBasso e dominanzaCerchio"
```

---

## Task 5: GeneratoreInsight — regole crescitaTrend e decisioneStorica (TDD)

**Files:**
- Test: `EquinozioTests/GeneratoreInsightTests.swift` (estende)
- Modify: `Equinozio/Domain/GeneratoreInsight.swift`

- [ ] **Step 1: Aggiungere i test che falliscono**

Aggiungere dentro `struct GeneratoreInsightTests`:

```swift
    @Test func crescitaTrendQuandoEquilibrioMigliora() {
        let recente = rifl(30, 25, 25, 20)   // equilibrio più alto
        let vecchia = rifl(60, 20, 10, 10)   // equilibrio più basso
        let insight = GeneratoreInsight.genera(riflessioni: [recente, vecchia], decisioni: [], adesso: .now)
        #expect(insight.contains { $0.tipo == .crescitaTrend })
    }

    @Test func nessunaCrescitaConUnaSolaRiflessione() {
        let insight = GeneratoreInsight.genera(riflessioni: [rifl(25, 25, 25, 25)], decisioni: [], adesso: .now)
        #expect(!insight.contains { $0.tipo == .crescitaTrend })
    }

    @Test func decisioneStoricaQuandoCeNeSonoInScadenza() {
        let traDueGiorni = Date(timeIntervalSince1970: 1_000_000 + 2 * 86_400)
        let adesso = Date(timeIntervalSince1970: 1_000_000)
        let d = Decisione(titolo: "Offerta", scadenza: traDueGiorni)
        let insight = GeneratoreInsight.genera(riflessioni: [rifl(25, 25, 25, 25)], decisioni: [d], adesso: adesso)
        #expect(insight.contains { $0.tipo == .decisioneStorica })
    }

    @Test func nessunaDecisioneStoricaSeDecisioneChiusa() {
        let traDueGiorni = Date(timeIntervalSince1970: 1_000_000 + 2 * 86_400)
        let adesso = Date(timeIntervalSince1970: 1_000_000)
        let d = Decisione(titolo: "Offerta", scadenza: traDueGiorni)
        d.decisione = "Accettato"   // chiusa
        let insight = GeneratoreInsight.genera(riflessioni: [rifl(25, 25, 25, 25)], decisioni: [d], adesso: adesso)
        #expect(!insight.contains { $0.tipo == .decisioneStorica })
    }
```

- [ ] **Step 2: Eseguire e verificare il fallimento**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests/GeneratoreInsightTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: FALLISCE — le nuove regole non sono ancora chiamate.

- [ ] **Step 3: Aggiungere le due regole e collegarle in `genera`**

In `Equinozio/Domain/GeneratoreInsight.swift`, in `genera(...)` aggiungere le chiamate dopo `dominanzaCerchio`:

```swift
        if let i = crescitaTrend(riflessioni) { risultato.append(i) }
        if let i = decisioneStorica(decisioni, adesso: adesso) { risultato.append(i) }
```

e aggiungere i due metodi dentro l'enum (dopo `dominanzaCerchio`):

```swift
    static func crescitaTrend(_ riflessioni: [Riflessione]) -> InsightGenerato? {
        guard riflessioni.count >= 2 else { return nil }
        let delta = riflessioni[0].equilibrio - riflessioni[1].equilibrio
        guard delta > 0 else { return nil }
        return InsightGenerato(
            tipo: .crescitaTrend,
            testo: "Il tuo equilibrio è in crescita: +\(delta) rispetto alla settimana scorsa."
        )
    }

    static func decisioneStorica(_ decisioni: [Decisione], adesso: Date) -> InsightGenerato? {
        let aperte = decisioni.filter { ($0.decisione ?? "").isEmpty }
        guard !aperte.isEmpty else { return nil }

        let limite = adesso.addingTimeInterval(7 * 86_400)
        let inScadenza = aperte.filter { d in
            guard let scadenza = d.scadenza else { return false }
            return scadenza <= limite
        }

        if !inScadenza.isEmpty {
            let n = inScadenza.count
            return InsightGenerato(
                tipo: .decisioneStorica,
                testo: "Hai \(n) decision\(n == 1 ? "e" : "i") in scadenza. Forse è il momento di chiuderne una."
            )
        }
        if aperte.count >= 3 {
            return InsightGenerato(
                tipo: .decisioneStorica,
                testo: "Hai \(aperte.count) decisioni aperte: rischi di accumularle."
            )
        }
        return nil
    }
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests/GeneratoreInsightTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: tutti i test del generatore (8) passano.

- [ ] **Step 5: Commit**

```bash
git add Equinozio/Domain/GeneratoreInsight.swift EquinozioTests/GeneratoreInsightTests.swift
git commit -m "feat: GeneratoreInsight con regole crescitaTrend e decisioneStorica"
```

---

## Task 6: Mostrare gli Insight nella Mappa

> Scelta di design: gli insight sono calcolati al volo dai dati già interrogati (deterministici, costo trascurabile) e NON persistiti nel modello `Insight`. Niente lifecycle di cache da gestire (YAGNI); il modello `Insight` resta nello schema, invariato.

**Files:**
- Create: `Equinozio/Features/Mappa/BloccoInsight.swift`
- Modify: `Equinozio/Features/Mappa/MappaView.swift`

- [ ] **Step 1: Creare la vista BloccoInsight**

Create `Equinozio/Features/Mappa/BloccoInsight.swift`:

```swift
//
//  BloccoInsight.swift
//  Equinozio · Features · Mappa
//
//  Mostra gli insight generati (compagno discreto) nella dashboard.
//

import SwiftUI

struct BloccoInsight: View {
    let insight: [InsightGenerato]

    var body: some View {
        if !insight.isEmpty {
            VStack(alignment: .leading, spacing: S.x3) {
                Text("SPUNTI")
                    .font(.equinozio(.etichetta))
                    .tracking(2.2)
                    .foregroundStyle(Color.attenuato)

                VStack(spacing: S.x2) {
                    ForEach(insight) { spunto in
                        HStack(alignment: .top, spacing: S.x3) {
                            Image(systemName: icona(spunto.tipo))
                                .font(.system(size: 16, weight: .light))
                                .foregroundStyle(Color.salvia)
                                .frame(width: 24, height: 24, alignment: .center)
                                .padding(.top, 2)

                            Text(spunto.testo)
                                .font(.equinozio(.corpoMedio))
                                .foregroundStyle(Color.inchiostro)
                                .frame(maxWidth: .infinity, alignment: .leading)
                        }
                        .padding(S.x4)
                        .frame(maxWidth: .infinity, alignment: .leading)
                        .background(Color.superficie)
                        .clipShape(RoundedRectangle(cornerRadius: R.r2))
                        .overlay(
                            RoundedRectangle(cornerRadius: R.r2)
                                .stroke(Color.lineaSottile, lineWidth: 1)
                        )
                    }
                }
            }
        }
    }

    private func icona(_ tipo: TipoInsight) -> String {
        switch tipo {
        case .bilanciamentoBasso: return "exclamationmark.circle"
        case .dominanzaCerchio:   return "circle.lefthalf.filled"
        case .crescitaTrend:      return "chart.line.uptrend.xyaxis"
        case .decisioneStorica:   return "scale.3d"
        }
    }
}
```

- [ ] **Step 2: Calcolare e inserire gli insight in MappaView**

In `Equinozio/Features/Mappa/MappaView.swift`:

(a) aggiungere una computed property dopo `decisioniAperte` (riga ~32):

```swift
    private var insight: [InsightGenerato] {
        GeneratoreInsight.genera(riflessioni: riflessioni, decisioni: decisioni, adesso: .now)
    }
```

(b) nel `body`, dentro la `VStack`, inserire il blocco dopo `bloccoEquilibrio` e prima di `bloccoDiagramma`:

```swift
                BloccoInsight(insight: insight)
```

- [ ] **Step 3: Build**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild -scheme Equinozio -destination 'generic/platform=iOS Simulator' -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -4`
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 4: Commit**

```bash
git add Equinozio/Features/Mappa/BloccoInsight.swift Equinozio/Features/Mappa/MappaView.swift
git commit -m "feat: spunti (insight) nella Mappa generati dai dati dell'utente"
```

---

## Task 7: Suggerimento tag anche in modifica del Diario

Replicare in `DettaglioPaginaView` (modalità modifica) il bottone "SUGGERISCI" già presente nel composer.

**Files:**
- Modify: `Equinozio/Features/Diario/DiarioView.swift`

- [ ] **Step 1: Aggiungere lo stato `suggerendo`**

In `DettaglioPaginaView` (dopo `@State private var mostraConfermaCancella = false`, riga ~352):

```swift
    @State private var suggerendo = false
```

- [ ] **Step 2: Aggiungere il bottone SUGGERISCI nell'header "CERCHI ASSOCIATI"**

In `DettaglioPaginaView.body`, sostituire l'header delle etichette (le righe):

```swift
                        Text("CERCHI ASSOCIATI")
                            .font(.equinozio(.etichetta))
                            .tracking(1.8)
                            .foregroundStyle(Color.attenuato)
```

con:

```swift
                        HStack {
                            Text("CERCHI ASSOCIATI")
                                .font(.equinozio(.etichetta))
                                .tracking(1.8)
                                .foregroundStyle(Color.attenuato)

                            Spacer()

                            if inModifica {
                                Button {
                                    Task { await suggerisciEtichette() }
                                } label: {
                                    HStack(spacing: 4) {
                                        if suggerendo {
                                            ProgressView().scaleEffect(0.6).frame(width: 12, height: 12)
                                        } else {
                                            Image(systemName: "sparkles")
                                                .font(.system(size: 11, weight: .medium))
                                        }
                                        Text("SUGGERISCI")
                                            .font(.equinozio(.etichetta))
                                            .tracking(1.2)
                                    }
                                    .foregroundStyle(Color.salvia)
                                }
                                .buttonStyle(.plain)
                                .disabled(suggerendo || testoEditabile.trimmingCharacters(in: .whitespaces).isEmpty)
                                .opacity(testoEditabile.trimmingCharacters(in: .whitespaces).isEmpty ? 0.4 : 1)
                            }
                        }
```

- [ ] **Step 3: Aggiungere il metodo `suggerisciEtichette`**

In `DettaglioPaginaView`, dopo `avviaModifica()` (riga ~488):

```swift
    private func suggerisciEtichette() async {
        suggerendo = true
        defer { suggerendo = false }
        let suggerite = await TagSuggestionService.shared.suggerisci(per: testoEditabile)
        etichetteEditabili = Set(suggerite)
    }
```

- [ ] **Step 4: Build**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild -scheme Equinozio -destination 'generic/platform=iOS Simulator' -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -4`
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 5: Commit**

```bash
git add Equinozio/Features/Diario/DiarioView.swift
git commit -m "feat: suggerimento tag anche in modifica della pagina del Diario"
```

---

## Task 8: Modifica delle riflessioni passate nello Storico

Rendere ogni riga dello Storico toccabile per aprire uno sheet di modifica (quattro quote con vincolo 100% + pensiero).

**Files:**
- Create: `Equinozio/Features/Riflessione/ModificaRiflessioneView.swift`
- Test: `EquinozioTests/RiflessioneTests.swift` (estende)
- Modify: `Equinozio/Features/Riflessione/StoricoRiflessioniView.swift`

- [ ] **Step 1: Scrivere il test di round-trip della modifica**

Aggiungere dentro `struct RiflessioneTests`:

```swift
    @MainActor
    @Test func modificaQuotePersisteEAggiornaEquilibrio() throws {
        let schema = Schema([Profilo.self, Cerchio.self, Elemento.self, Pagina.self, Riflessione.self, Decisione.self, Insight.self])
        let container = try ModelContainer(
            for: schema,
            configurations: [ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)]
        )
        let ctx = container.mainContext

        let r = Riflessione(data: .now, quotaPassione: 70, quotaTalento: 10, quotaMissione: 10, quotaProfessione: 10)
        ctx.insert(r)
        try ctx.save()

        // Modifica verso il bilanciamento perfetto
        r.quotaPassione = 25; r.quotaTalento = 25; r.quotaMissione = 25; r.quotaProfessione = 25
        r.pensiero = "Riequilibrata"
        try ctx.save()

        let letta = try ctx.fetch(FetchDescriptor<Riflessione>()).first
        #expect(letta?.equilibrio == 100)
        #expect(letta?.pensiero == "Riequilibrata")
    }
```

- [ ] **Step 2: Eseguire e verificare**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests/RiflessioneTests/modificaQuotePersisteEAggiornaEquilibrio CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: PASS (verifica che la modifica diretta del model persista e che `equilibrio` si ricalcoli). Conferma la logica che la UI userà.

- [ ] **Step 3: Creare lo sheet di modifica**

Create `Equinozio/Features/Riflessione/ModificaRiflessioneView.swift`:

```swift
//
//  ModificaRiflessioneView.swift
//  Equinozio · Features · Riflessione
//
//  Sheet per correggere una riflessione passata: quote (somma 100%) + pensiero.
//

import SwiftUI
import SwiftData

struct ModificaRiflessioneView: View {

    @Environment(\.modelContext) private var contesto
    @Environment(\.dismiss) private var chiudi
    @Bindable var riflessione: Riflessione

    @State private var passione: Int = 25
    @State private var talento: Int = 25
    @State private var missione: Int = 25
    @State private var professione: Int = 25
    @State private var pensiero: String = ""
    @State private var caricato = false

    private var totale: Int { passione + talento + missione + professione }

    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(alignment: .leading, spacing: S.x4) {
                    Text(dataFormattata(riflessione.data).uppercased())
                        .font(.equinozio(.etichetta))
                        .tracking(2.0)
                        .foregroundStyle(Color.attenuato)

                    VStack(spacing: S.x3) {
                        sliderRiga(.passione, valore: $passione)
                        sliderRiga(.talento, valore: $talento)
                        sliderRiga(.missione, valore: $missione)
                        sliderRiga(.professione, valore: $professione)
                    }

                    HStack {
                        Text("TOTALE")
                            .font(.equinozio(.etichetta))
                            .tracking(1.8)
                            .foregroundStyle(Color.attenuato)
                        Spacer()
                        Text("\(totale)%")
                            .font(.system(size: 22, weight: .thin))
                            .monospacedDigit()
                            .foregroundStyle(totale == 100 ? Color.salvia : Color.passione)
                    }
                    .padding(.vertical, S.x2)

                    TextEditor(text: $pensiero)
                        .font(.equinozio(.corpo))
                        .frame(minHeight: 80)
                        .scrollContentBackground(.hidden)
                        .padding(S.x2)
                        .background(Color.superficie)
                        .clipShape(RoundedRectangle(cornerRadius: R.r2))
                        .overlay(
                            RoundedRectangle(cornerRadius: R.r2)
                                .stroke(Color.lineaSottile, lineWidth: 1)
                        )
                        .accessibilityLabel("Pensiero della settimana")

                    AzionePrimaria("Salva le modifiche", azione: salva)
                        .disabled(totale != 100)
                        .opacity(totale == 100 ? 1 : 0.4)
                }
                .padding(.horizontal, S.x5)
                .padding(.top, S.x4)
                .padding(.bottom, S.x6)
            }
            .background(Color.sfondo)
            .toolbar {
                ToolbarItem(placement: .topBarLeading) {
                    Button("Annulla") { chiudi() }
                        .tint(.attenuato)
                }
                ToolbarItem(placement: .principal) {
                    Text("MODIFICA RIFLESSIONE")
                        .font(.equinozio(.etichetta))
                        .tracking(2.2)
                        .foregroundStyle(Color.salvia)
                }
            }
            .onAppear(perform: carica)
        }
    }

    private func sliderRiga(_ tipo: TipoCerchio, valore: Binding<Int>) -> some View {
        VStack(spacing: S.x2) {
            HStack(alignment: .firstTextBaseline) {
                HStack(spacing: S.x2) {
                    Circle().fill(tipo.colore).frame(width: 8, height: 8)
                    Text(tipo.titoloRiflessione)
                        .font(.equinozio(.corpoMedio))
                }
                Spacer()
                Text("\(valore.wrappedValue)%")
                    .font(.system(size: 17, weight: .thin))
                    .monospacedDigit()
            }
            Slider(value: Binding(
                get: { Double(valore.wrappedValue) },
                set: { nuovo in
                    let altri = totale - valore.wrappedValue
                    let limite = 100 - altri
                    valore.wrappedValue = min(Int(nuovo), limite)
                }
            ), in: 0...100, step: 1)
            .tint(tipo.colore)
        }
    }

    private func carica() {
        guard !caricato else { return }
        caricato = true
        passione = riflessione.quotaPassione
        talento = riflessione.quotaTalento
        missione = riflessione.quotaMissione
        professione = riflessione.quotaProfessione
        pensiero = riflessione.pensiero
    }

    private func salva() {
        guard totale == 100 else { return }
        riflessione.quotaPassione = passione
        riflessione.quotaTalento = talento
        riflessione.quotaMissione = missione
        riflessione.quotaProfessione = professione
        riflessione.pensiero = pensiero.trimmingCharacters(in: .whitespacesAndNewlines)
        try? contesto.save()
        chiudi()
    }

    private func dataFormattata(_ data: Date) -> String {
        let f = DateFormatter()
        f.locale = Locale(identifier: "it_IT")
        f.dateFormat = "EEEE d MMMM"
        return f.string(from: data)
    }
}
```

- [ ] **Step 4: Rendere le righe dello Storico toccabili**

In `Equinozio/Features/Riflessione/StoricoRiflessioniView.swift`:

(a) aggiungere lo stato di selezione dopo `@AppStorage("storicoIntroLetta")` (riga 16):

```swift
    @State private var inModifica: Riflessione?
```

(b) nella `lista`, avvolgere `rigaRiflessione(r)` in un Button. Sostituire:

```swift
                    ForEach(riflessioni) { r in
                        rigaRiflessione(r)
                    }
```

con:

```swift
                    ForEach(riflessioni) { r in
                        Button {
                            inModifica = r
                        } label: {
                            rigaRiflessione(r)
                        }
                        .buttonStyle(.plain)
                    }
```

(c) aggiungere lo sheet sul `ScrollView` (subito dopo `.background(Color.sfondo)`, riga 47):

```swift
            .sheet(item: $inModifica) { r in
                ModificaRiflessioneView(riflessione: r)
                    .presentationDetents([.large])
            }
```

- [ ] **Step 5: Build**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild -scheme Equinozio -destination 'generic/platform=iOS Simulator' -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -4`
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 6: Test**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20`
Expected: tutti i test verdi.

- [ ] **Step 7: Commit**

```bash
git add Equinozio/Features/Riflessione/ModificaRiflessioneView.swift Equinozio/Features/Riflessione/StoricoRiflessioniView.swift EquinozioTests/RiflessioneTests.swift
git commit -m "feat: modifica delle riflessioni passate nello Storico"
```

---

## Task 9: Verifica finale

**Files:** nessuno.

- [ ] **Step 1: Suite completa unit test**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:EquinozioTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -30`
Expected: tutti i test verdi (equilibrio, pensiero, modifica, 8 del generatore).

- [ ] **Step 2: Build pulita**

Run: `cd /Users/giuseppe/XCode/Equinozio && xcodebuild -scheme Equinozio -destination 'generic/platform=iOS Simulator' -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -4`
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 3: Controllo visivo (manuale, sul device dell'utente)**

Poiché l'app usa CloudKit e non si avvia nel simulatore senza iCloud, il controllo visivo va fatto sul device:
- Riflessione: c'è il campo pensiero; salvando, ricompare allo Storico.
- Mappa: compaiono gli "Spunti" quando ci sono riflessioni/decisioni che attivano una regola.
- Diario: in modifica di una pagina appare "SUGGERISCI" e popola i cerchi.
- Storico: toccando una riga si apre la modifica; salvando aggiorna il valore/grafico.

---

## Self-Review

- **Copertura spec (sez. 4, Tema 1):** (1) pensiero → Task 3 ✓ · (2) Insight generati + Mappa → Task 4/5/6 ✓ · (3) tag in modifica Diario → Task 7 ✓ · (4) modifica riflessioni passate → Task 8 ✓ · (5) radar live → **già funzionante** (verificato: `RadarDecisione` è una View pura legata a `@State` reattivi), nessun task, annotato.
- **Placeholder:** nessun TBD/TODO; ogni step ha codice completo e comandi con output atteso.
- **Coerenza tipi:** `Riflessione.equilibrio(passione:talento:missione:professione:)` (Task 2) usato in Task 4/5 (via `.equilibrio`). `InsightGenerato`/`TipoInsight`/`GeneratoreInsight.genera(riflessioni:decisioni:adesso:)` definiti in Task 4 e usati invariati in Task 5/6. `ModificaRiflessioneView(riflessione:)` (Task 8). `TagSuggestionService.shared.suggerisci(per:)` come nel composer esistente.
- **Premessa di esecuzione:** Task 1 deve girare per primo (abilita i test); se la sua verifica fallisce, applicare la contingenza documentata prima di proseguire.
- **Nota onestà test:** i test coprono la logica pura e i round-trip SwiftData in-memory; il wiring SwiftUI è verificato via build + controllo visivo su device.
```
