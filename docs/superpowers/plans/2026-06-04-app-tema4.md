# Equinozio App — Tema 4 (Gancio Systema360) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Inserire, con gusto e in modo discreto, un gancio verso Systema360 dentro l'app: una sezione "Chi l'ha fatta" curata con invito allo studio, e un "Condividi Equinozio" per il passaparola.

**Architecture:** Modifiche puntuali a `ImpostazioniView.swift` (la sezione finale "Equinozio · Da Systema360"), verificate via build. Coerente con il tono "compagno discreto".

**Tech Stack:** SwiftUI. Lavorare su `main`.

**Spec:** `docs/superpowers/specs/2026-06-03-equinozio-landing-e-app-design.md` (sez. 4, Tema 4).

---

## Convenzioni
- **Build:** `xcodebuild -scheme Equinozio -destination 'generic/platform=iOS Simulator' -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -4` → `** BUILD SUCCEEDED **`
- **Test:** `xcodebuild test -scheme Equinozio -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:EquinozioTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -6`
- Token `S.*`, `R.*`, `Color.*`, `Font.equinozio`. Mai bold.

## File Structure
**Modificati:** `Equinozio/Features/Impostazioni/ImpostazioniView.swift` — sezione finale arricchita con "Condividi Equinozio" + invito curato allo studio.

---

## Task 1: "Condividi Equinozio" + invito Systema360

La sezione esistente è `sezione(occhiello: "Equinozio", titolo: "Da Systema360, con cura") { ... }` con: bottone manifesto, "Versione 1.0 (1)", link systema360.it.

**Files:** `Equinozio/Features/Impostazioni/ImpostazioniView.swift`

- [ ] **Step 1: Aggiungere "Condividi Equinozio" (ShareLink) dopo il bottone manifesto**

Subito DOPO il blocco `Button { manifestoAperto = true } label: { ... }.buttonStyle(.plain)` (dentro la sezione "Equinozio"), inserisci:

```swift
                        ShareLink(
                            item: URL(string: "https://systema360.it")!,
                            subject: Text("Equinozio"),
                            message: Text("Equinozio — uno strumento sobrio per bilanciare i quattro cerchi della tua vita. Gratis, da Systema360.")
                        ) {
                            HStack {
                                Image(systemName: "square.and.arrow.up")
                                    .font(.system(size: 18, weight: .light))
                                    .foregroundStyle(Color.salvia)
                                    .frame(width: 28)
                                Text("Condividi Equinozio")
                                    .font(.equinozio(.corpo))
                                    .foregroundStyle(Color.inchiostro)
                                Spacer()
                                Image(systemName: "chevron.right")
                                    .font(.system(size: 12, weight: .medium))
                                    .foregroundStyle(Color.attenuato)
                            }
                            .padding(S.x4)
                            .background(Color.superficie)
                            .clipShape(RoundedRectangle(cornerRadius: R.r1))
                            .overlay(
                                RoundedRectangle(cornerRadius: R.r1)
                                    .stroke(Color.lineaSottile, lineWidth: 1)
                            )
                        }
                        .buttonStyle(.plain)
                        .padding(.top, S.x2)
```

- [ ] **Step 2: Aggiungere l'invito curato allo studio prima del link systema360.it**

Subito PRIMA del blocco `Link(destination: URL(string: "https://systema360.it")!) { ... }`, inserisci:

```swift
                        Text("Equinozio è il biglietto da visita di Systema360, studio di consulenza e progettazione digitale a Potenza. Se ti serve un sistema digitale curato così, parliamone.")
                            .font(.equinozio(.corpoMedio))
                            .foregroundStyle(Color.inchiostroTenue)
                            .fixedSize(horizontal: false, vertical: true)
                            .padding(.top, S.x3)
```

- [ ] **Step 3: Build** → BUILD SUCCEEDED.

- [ ] **Step 4: Commit**

```bash
git add Equinozio/Features/Impostazioni/ImpostazioniView.swift
git commit -m "feat: Condividi Equinozio + invito curato a Systema360 nelle Impostazioni"
```

## Notes
- `manifestoAperto`, la sezione "Equinozio", il `Link` a systema360.it e "Versione 1.0 (1)" esistono già — solo aggiunte.
- `ShareLink(item: URL, subject:message:)` è API SwiftUI standard (iOS 16+). Niente bold; usa weight .light/.medium come il resto della sezione.
- Lasciare invariate le altre sezioni.

---

## Task 2: Verifica finale

- [ ] **Step 1: Build** → `** BUILD SUCCEEDED **`.
- [ ] **Step 2: Test** (no regressioni) → `** TEST SUCCEEDED **`.
- [ ] **Step 3: Controllo visivo (device):** in Impostazioni → sezione Equinozio: "Condividi Equinozio" apre il foglio di condivisione; compare l'invito allo studio sopra il link systema360.it.

---

## Self-Review
- **Copertura spec (Tema 4):** "Chi l'ha fatta" / invito allo studio → Task 1 Step 2 ✓ · "Condividi Equinozio" → Task 1 Step 1 ✓.
- **Placeholder:** nessuno. (Lo share usa l'URL dello studio finché l'app non è pubblicata; aggiornare all'URL App Store dopo la pubblicazione — coerente con la checklist submit.)
- **Coerenza:** solo aggiunte a una sezione esistente; nessun nuovo tipo; token e tono rispettati (discreto).
- **Onestà:** verifica via build + controllo visivo su device.
```
